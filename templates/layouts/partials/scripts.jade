//- Bootstrap core JavaScript
//- Placed at the end of the document so the pages load faster
script(src='https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js')
script(src='/js/jquery-ui.js')
script(src='/js/bootstrap.js')
script(src='https://cdnjs.cloudflare.com/ajax/libs/jquery-instagram/0.3.1/instagram.js')
script(src='/js/plugins/masterslider/masterslider.min.js')
script(src='/js/bower_components/instafeed.js/instafeed.js')
script(src='/js/bower_components/moment/moment.js')
if getLocale(req) !== 'en'
	script(src='/js/bower_components/moment/locale/'+getLocale(req)+'.js')
script(src='/js/script.js')
	

//- The KeystoneJS Content Editor provides support for ks-editable data attributes,
//- which generate links to edit content for users who can access Keystone
if user && user.canAccessKeystone
	script(src='/keystone/js/content/editor.js')

//- Add scripts that are globally required by your site here.

script(type="text/javascript").
	$(function () {
		window.setInterval(function(){
			$("#clock").html(moment().format("DD MMMM YYYY HH:mm"));
		}, 1000);
	});
